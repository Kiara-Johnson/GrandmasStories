const storyReducer = (story = [], actions) => {
    switch (actions.type) {
        case 'value':
            
            break;
    
        default: 
           return story
    }
}
export default storyReducer