const vasReducer = (vas = [], actions) => {
    switch (actions.type) {
        case 'value':
            
            break;
    
        default: 
           return vas
    }
}
export default vasReducer